# Dependencies
node_modules
.pnp
.pnp.js

# Build output
dist
dist-ssr
*.local
*.tsbuildinfo

# Environment & secrets — NEVER commit real secrets. Only .env.example is tracked.
.env
.env.*
!.env.example


# Supabase local state & secrets
supabase/.branches
supabase/.temp
supabase/.env

# Testing artifacts
coverage
playwright-report
test-results
blob-report
.nyc_output

# Editor / OS
.vscode/*
!.vscode/extensions.json
# Local agent config + ephemeral worktrees — never commit (embedded git repos).
.claude/
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
Thumbs.db

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Design handoff reference — do not commit
design_handoff_aretim/
.claude/launch.json

# Internal process notes — do not commit
process/
